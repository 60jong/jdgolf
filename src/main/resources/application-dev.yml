server:
  port: 9000
  servlet:
    session:
      tracking-modes: cookie
spring:
  application:
    name: jdgolf
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: root
    password: 991911
  thymeleaf:
    cache: false
  jpa:
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: true
